# Install PyTorch (choose the variant matching your CUDA version)
# CUDA 12.1:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# CUDA 12.4:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# CPU only:
#   pip install torch torchaudio

# Core dependencies
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0
accelerate>=0.28.0
evaluate>=0.4.0
jiwer>=3.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
numpy<2.0

# Training utilities
tensorboard>=2.15.0
tqdm>=4.66.0
huggingface_hub>=0.22.0
matplotlib
# Text Processing & Tokenization
sentencepiece
transformers
editdistance
webdataset
# Interactive / Notebooks
ipywidgets
jupyterlab
